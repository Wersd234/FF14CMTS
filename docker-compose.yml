version: '3.8'

services:
  ff14-brain:
    build: .
    container_name: ff14-brain
    restart: unless-stopped
    ports:
      - "11035:8000"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]